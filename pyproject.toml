[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "VaaniRakshak-AI"
version = "0.1.0"
description = "VaaniRakshak-AI dataset-engineering foundation for synthetic-speech detection."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "pandas",
    "torch",
    "torchaudio",
    "soundfile",
    "scipy",
    "pyyaml",
    "tqdm",
]

[project.optional-dependencies]
dev = ["pytest"]
analysis = ["matplotlib"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
